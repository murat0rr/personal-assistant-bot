// Событийная шина виджетов аналитики (Phase 46) — единственный способ,
// которым остальной код (задачи, проекты) должен затрагивать виджеты:
// публикует событие, не вызывает функцию виджета по имени напрямую.
// Виджет, которому это неинтересно, просто не подписывается — ничего
// не падает и не требует правки в двух местах при добавлении/удалении
// виджета. Обычный EventTarget — в браузере он есть из коробки, тащить
// сюда библиотеку не за чем.
export const bus = new EventTarget();
